# Paste these into Streamlit Cloud → your app → Settings → Secrets.
# Do NOT commit a real secrets.toml — it's gitignored.

SUPABASE_URL = "https://YOUR-PROJECT.supabase.co"
SUPABASE_ANON_KEY = "eyJhbGciOi..."   # anon/public key, NOT service_role
